name = "mev-fortran"
version = "0.3.0"
license = "GPL-3.0-only"
author = "mev authors; Fortran translation"
maintainer = ""
copyright = "See NOTICE.md"
categories = ["statistics", "numerical"]
keywords = ["extreme-value", "statistics", "numerical", "statistical-computing"]

[library]
source-dir = "src"

[dependencies]
nleqslv-fortran = { path = "../nleqslv" }
expint-fortran = { path = "../expint" }

[build]
auto-executables = true
auto-tests = true
auto-examples = true

[extra.r-fpm-index]
gams = ["L"]
