# Licenses

The upstream `DiscreteWeibull` package declares `License: GPL`.

The supplied `Rsolnp-fortran` dependency is GPL-2.0-only. The combined FPM
archive is therefore distributed as **GPL-2.0-only**, with a copy of GPL-2 in
`LICENSE`.

The complete supplied R package is retained under `upstream/`; the supplied
optimizer translation is retained under `vendor/Rsolnp-fortran/`.
