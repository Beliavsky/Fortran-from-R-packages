# Upstream provenance

This project is a modern Fortran translation of:

- **nls2 0.3-4**
- Author/Maintainer in the supplied package: G. Grothendieck
- Upstream URL in `DESCRIPTION`: `https://github.com/ggrothendieck/nls2`
- Upstream package license: `GPL-2`

The complete uploaded source tree is retained under:

```text
original/nls2-master/
```

The upstream archive also contains `R/nls.R`, derived from the R `stats` package and carrying its own GPL version-2-or-later copyright/header notices from the R Core Team and original NLS authors. Those original notices are retained unchanged in `original/`.

The translated source files carry SPDX `GPL-2.0-only` headers to match the package-level `DESCRIPTION` declaration. This distribution also includes a copy of GPL version 2 in `LICENSE`.
