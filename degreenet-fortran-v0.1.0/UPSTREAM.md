# Upstream provenance

This project is a source translation of the uploaded CRAN-style source tree
`degreenet` 1.3-7, not a clean-room reimplementation.

Upstream package metadata and citation material are retained under `docs/`.
The original `LICENSE` is retained at the package root.

Required attribution from the upstream license:

> Based on 'statnet' project software (statnet.org).
> For license and citation information see statnet.org/attribution
