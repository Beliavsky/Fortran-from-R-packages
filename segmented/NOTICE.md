# License and provenance notice

The original `segmented` package is by Vito M. R. Muggeo and contributors and is
distributed under `License: GPL`. In R package metadata this denotes GPL version
2 or version 3. The mixed-effects implementation in this project adapts the
previous `nlme-fortran` translation, whose upstream `nlme` code is
GPL-2.0-or-later.

The combined Fortran project is therefore distributed under
**GPL-2.0-or-later**. The full GPL version 2 text is in `LICENSE`. Every compiled
Fortran source, test, example, and demo carries an SPDX license identifier.

The original package metadata, NEWS file, namespace, and R sources are retained
under `original/`. No authorship of the upstream statistical methods is claimed
by the translator.
