# Upstream provenance

Upstream package: `coneproj` 1.23

Upstream DESCRIPTION declares:

```text
License: GPL (>= 2)
```

Translated source files therefore carry `SPDX-License-Identifier: GPL-2.0-or-later`.
The complete uploaded upstream package is retained under `original/coneproj-master/`.

The C++ upstream implementation uses Rcpp and RcppArmadillo. Those libraries are not copied or linked by this standalone Fortran translation.
