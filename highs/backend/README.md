# Backend build output

`scripts/build_backend.bat` writes the Windows DLL and runtime dependencies to `backend/bin`.

`scripts/build_backend.sh` writes the Unix shared library to `backend/lib`.

The generated HiGHS build and install trees are intentionally excluded from the source archive.
