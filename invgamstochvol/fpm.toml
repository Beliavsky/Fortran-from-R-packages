name = "invgamstochvol"
version = "1.0.0"
license = "MIT"
author = "Leon Gonzalez, Blessings Majoni; Fortran translation"
maintainer = "Fortran translation project"
description = "Exact likelihood and posterior smoothing for inverse-gamma stochastic volatility models"
categories = ["statistics", "time-series", "finance"]
gams = ["L"]
keywords = ["stochastic-volatility", "inverse-gamma", "likelihood", "smoothing"]

[library]
source-dir = "src"

[build]
auto-executables = true
auto-examples = true
auto-tests = true
module-naming = false

[install]
library = true
