# Notices

The R package `nnls` is distributed under GPL (>= 2). This translation is
therefore distributed under GPL-2.0-or-later.

The upstream package also identifies the original Lawson-Hanson NNLS source as
public-domain code from Netlib. See `COPYRIGHTS` and
`original/nnls-master/inst/COPYRIGHTS` for the upstream notice.
