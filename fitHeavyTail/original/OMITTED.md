# Omitted R infrastructure

The modern Fortran project omits `R/plotting.R`, generated package startup and
roxygen infrastructure, vignettes, HTML rendering, CRAN metadata workflows,
and saved `.RData` regression fixtures. Plotting is outside the requested
scope. The numerical algorithms used by the exported estimators and their
computational helper routines are translated.
