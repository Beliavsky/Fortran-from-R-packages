# Upstream provenance

## quadprogXT

Source archive supplied by the user: `quadprogXT-master.zip`.

Upstream metadata:

- Package: `quadprogXT`
- Version: `0.0.6`
- Authors: Bob Harlow; Brian Koch (contributor)
- License declaration: `GPL (>= 2)`
- NeedsCompilation: `no`

The complete supplied archive is retained under `original/quadprogXT-master/`.

## quadprog-fortran

Source archive supplied by the user: `quadprog-fortran(3).zip`.

The dependency is retained under `vendor/quadprog-fortran/`, including its own
license, API, porting, testing, and upstream provenance material.

The only local dependency patch is the exact-real comparison syntax described
in `NOTICE.md` and `TRANSLATION_COVERAGE.md`.
