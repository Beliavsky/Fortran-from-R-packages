# Notice

This is a Fortran translation of the computational code in limSolve 2.0.3.
The upstream package declares `License: GPL` without a version qualifier.  The
complete supplied package is retained under `original/limSolve-master`.

The default FPM build also links vendored translations of lpSolve
(LGPL-2.0-only) and quadprog (GPL-2.0-or-later).  Their license files and source
provenance remain in their respective vendor directories.
