# Notices

The translated package-level code is distributed under GPL-2.0-or-later,
following the upstream R package declaration `GPL (>= 2)`.

The Lawson-Hanson routines included in the original source tree are described
by the upstream package as public domain.  Their original comments and
attribution are preserved in `original/lsei-master/src/`.
