# NOTICE

This project is a modern Fortran translation of computational routines from the R package `greybox` version 2.0.8.

Upstream package metadata:

- Package: greybox
- Version: 2.0.8
- Copyright: upstream greybox authors
- License declared by current `DESCRIPTION`: LGPL-2.1

The current upstream `DESCRIPTION` is treated as the authoritative license declaration. An older package-level documentation comment in `R/greybox-package.R` still mentions GPL-2; that historical text is retained in `orig/` and is not used to broaden or change the current package license.

The original R and C++ source files used for translation are retained under `orig/` for provenance. The Fortran translation is distributed under LGPL-2.1-only, consistent with the current upstream package metadata.
