# Licensing

The supplied upstream package metadata (`DESCRIPTION`) states:

```text
License: GPL
```

No GPL version qualifier is present in the supplied package metadata, so this port does not invent one. The original supplied `lmomco-master.zip` is included under `upstream/` so the precise upstream source and metadata remain available for audit and attribution.

The Fortran translation is intended to preserve the upstream licensing conditions applicable to the translated code.
