! SPDX-License-Identifier: GPL-3.0-only
module evd
   use r_compat, only : dp
   use evd_univariate
   use evd_transform
   use evd_bivariate
   use evd_multivariate
   use evd_simulation
   use evd_process
   use evd_nonparametric
   use evd_bvpot
   use evd_fit
   implicit none
   public
end module evd
