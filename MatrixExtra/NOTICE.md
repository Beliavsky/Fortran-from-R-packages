# Licensing notice

This translation is derived from MatrixExtra 0.1.15, whose DESCRIPTION declares
`GPL (>= 2)` and whose source archive includes the GNU GPL version 3 license text.

The package also vendors the previously supplied `Matrix-fortran` translation as
an FPM dependency. That translation is distributed under GPL-3.0-only. Therefore
the linked default `matrixextra-fortran` target is distributed under GPL-3.0-only.

The original MatrixExtra source tree is preserved under `original/MatrixExtra-master/`.
Its `inst/COPYRIGHTS` also records the MIT license for the optional robin-map code.
The Fortran translation does not use robin-map.
