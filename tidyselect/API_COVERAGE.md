# Translation coverage

`translation_status: partial`

`frac_functions_translated: 16/29 (0.552)`

Direct mappings are `all_of`, `any_of`, `contains`, `ends_with`,
`eval_relocate`, `eval_rename`, `eval_select`, `everything`, `last_col`,
`matches`, `num_range`, `one_of`, `starts_with`, `vars_pull`, `vars_rename`,
and `vars_select`. `matches` currently means fixed substring matching.
