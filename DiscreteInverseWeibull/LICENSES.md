# Licenses

The supplied `DiscreteInverseWeibull` package declares `License: GPL`. Its original source is retained under `upstream/DiscreteInverseWeibull-master`.

The supplied `Rsolnp-fortran` dependency is GPL-2.0-only and is retained under `vendor/Rsolnp-fortran`. Because the distributed FPM project links that dependency, this combined archive is marked GPL-2.0-only.
