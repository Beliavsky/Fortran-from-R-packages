# Upstream provenance

Translated from the user-supplied archive `onls-master.zip`.

Upstream metadata:

- Package: `onls`
- Version: `0.1-4`
- Title: Orthogonal Nonlinear Least-Squares Regression
- Author/Maintainer: Andrej-Nikolai Spiess
- Declared license: `GPL (>= 2)`
- Upstream dependency used for fitting: `minpack.lm`

The upstream DESCRIPTION states that the package delivers the same results as
ODRPACK (Boggs et al., 1989) but is implemented in pure R.  This Fortran project
translates the supplied R algorithm; it is not a direct translation of ODRPACK.

The full supplied tree is preserved in `original/onls-master/`.
