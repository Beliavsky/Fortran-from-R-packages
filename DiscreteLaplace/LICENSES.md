# Licensing

The supplied `DiscreteLaplace` 1.1.1 DESCRIPTION declares:

```text
License: GPL
```

This translation preserves that declaration in `fpm.toml` and retains the
complete supplied package under `upstream/` for provenance.  No third-party
runtime code is vendored or linked by the translated library.
