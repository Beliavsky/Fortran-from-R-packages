# Upstream material not copied

The original compiled shared library `src/mixtools.so` was intentionally
excluded. Plotting code remains in the retained R source for reference but is
not translated into the compiled Fortran library. Bundled `.RData` datasets,
vignettes, generated HTML, and R package build artifacts are not required by
the FPM project and were not copied.
