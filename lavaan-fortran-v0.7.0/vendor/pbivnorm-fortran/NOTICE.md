# Notice

This is a modern Fortran translation of `pbivnorm` 0.6.0.

Upstream authorship states:
- Fortran code by Alan Genz.
- R code by Brenton Kenkel, based on Adelchi Azzalini's `mnormt` package.

The upstream package declares `License: GPL (>= 2)`. This translation is
therefore distributed under GPL-2.0-or-later.

Original package sources are retained under `orig/` for provenance.
