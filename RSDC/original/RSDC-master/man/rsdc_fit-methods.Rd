% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{rsdc_fit-methods}
\alias{rsdc_fit-methods}
\alias{print.rsdc_fit}
\alias{coef.rsdc_fit}
\alias{nobs.rsdc_fit}
\alias{logLik.rsdc_fit}
\alias{vcov.rsdc_fit}
\alias{confint.rsdc_fit}
\alias{summary.rsdc_fit}
\alias{predict.rsdc_fit}
\alias{simulate.rsdc_fit}
\alias{plot.rsdc_fit}
\title{Methods for fitted RSDC models}
\usage{
\method{print}{rsdc_fit}(x, digits = 4, ...)

\method{coef}{rsdc_fit}(object, ...)

\method{nobs}{rsdc_fit}(object, ...)

\method{logLik}{rsdc_fit}(object, ...)

\method{vcov}{rsdc_fit}(object, type = c("hessian", "opg", "sandwich", "bootstrap"), ...)

\method{confint}{rsdc_fit}(
  object,
  parm,
  level = 0.95,
  type = c("hessian", "opg", "sandwich", "bootstrap"),
  ...
)

\method{summary}{rsdc_fit}(object, ...)

\method{predict}{rsdc_fit}(
  object,
  residuals,
  sigma_matrix,
  value_cols,
  X = NULL,
  out_of_sample = FALSE,
  ...
)

\method{simulate}{rsdc_fit}(object, nsim = 1, seed = NULL, X = NULL, n = NULL, ...)

\method{plot}{rsdc_fit}(x, which = c("smoothed", "filtered"), ...)
}
\arguments{
\item{x, object}{An object of class \code{"rsdc_fit"}, as returned by
\code{\link{rsdc_estimate}}.}

\item{digits}{Number of significant digits for printing.}

\item{...}{Further arguments passed to methods (\code{vcov} forwards \code{B},
\code{X}, \code{seed}, and \code{cores} to \code{\link{rsdc_bootstrap}} when
\code{type = "bootstrap"}; otherwise unused).}

\item{type}{Covariance estimator: one of \code{"hessian"}, \code{"opg"},
\code{"sandwich"} (numerical) or \code{"bootstrap"} (simulation-based).}

\item{parm}{Vector of parameter names/indices (default: all).}

\item{level}{Confidence level.}

\item{residuals}{Numeric matrix \eqn{T \times K} of standardized residuals
on which the forecast filter is run.}

\item{sigma_matrix}{Numeric matrix of conditional standard deviations.}

\item{value_cols}{Columns of \code{sigma_matrix} giving the asset order.}

\item{X}{Covariate matrix \eqn{T \times p} (required for a \code{"tvtp"} fit;
\code{simulate} uses its row count as the simulation length).}

\item{out_of_sample}{Logical; passed to \code{\link{rsdc_forecast}}
(70/30 split).}

\item{nsim}{Unused (kept for generic compatibility; one path is returned).}

\item{seed}{Optional RNG seed.}

\item{n}{Series length for \code{"noX"}/\code{"const"} (defaults to the fitted T).}

\item{which}{Either \code{"smoothed"} (default) or \code{"filtered"}.}
}
\value{
\code{print} and \code{plot} return \code{x} invisibly. \code{coef},
\code{nobs}, \code{logLik}, \code{vcov} and \code{confint} return the usual
objects of those generics. \code{summary} returns a \code{"summary.rsdc_fit"}
list with a coefficient table (estimate, SE, z, p) and regime diagnostics.
\code{predict} returns the result of \code{\link{rsdc_forecast}}.
\code{simulate} returns a list with \code{states} and \code{observations}.
}
\description{
A model fitted with \code{\link{rsdc_estimate}} is returned as an object of
class \code{"rsdc_fit"}, which supports the standard fitted-model generics —
so it can be used like any other fitted model in R (\code{lm}, \code{glm},
\dots): print and summarize it, extract coefficients, standard errors and
confidence intervals, compute \code{AIC}/\code{BIC}, forecast, simulate new
data from the fitted process, and plot the regime probabilities. Each method
is described in the \emph{Functions} section below.
}
\section{Functions}{
\itemize{
\item \code{print(rsdc_fit)}: Compact printer for a fitted model.

\item \code{coef(rsdc_fit)}: Coefficient vector (named, in objective order).

\item \code{nobs(rsdc_fit)}: Number of observations used in estimation.

\item \code{logLik(rsdc_fit)}: Log-likelihood (carries \code{df} and \code{nobs} so
\code{stats::AIC}/\code{BIC} work out of the box).

\item \code{vcov(rsdc_fit)}: Variance-covariance matrix of the estimates. The
\emph{numerical} estimators are \code{type = "hessian"} (default, inverse
observed information), \code{"opg"} (outer product of gradients) and
\code{"sandwich"} (QML/robust \eqn{H^{-1} (\sum_t s_t s_t') H^{-1}}); the
\emph{simulation-based} estimator is \code{"bootstrap"}, which calls
\code{\link{rsdc_bootstrap}} (pass \code{B}, \code{X}, \code{seed}, and
\code{cores} through \code{...}). The bootstrap is recomputed on each call.

\item \code{confint(rsdc_fit)}: Wald confidence intervals from the chosen covariance
(numerical or \code{"bootstrap"}). For bootstrap \emph{percentile} intervals
instead of Wald, use \code{\link{rsdc_bootstrap}} directly.

\item \code{summary(rsdc_fit)}: Summary with a coefficient table (estimate, SE, z, p).

\item \code{predict(rsdc_fit)}: Forecast from a fitted model (wraps
\code{\link{rsdc_forecast}}); supply the residuals, conditional volatilities,
and (for \code{"tvtp"}) the covariate matrix \code{X}.

\item \code{simulate(rsdc_fit)}: Simulate from a fitted model. For \code{"tvtp"} supply
a covariate matrix \code{X} (its row count sets the length); for \code{"noX"}
the fixed transition matrix is used; for \code{"const"} a single regime is drawn.

\item \code{plot(rsdc_fit)}: Plot the smoothed regime probabilities (one panel per
regime) for a fitted \code{"noX"}/\code{"tvtp"} model. \code{which = "filtered"}
plots filtered probabilities instead.

}}
\examples{
\donttest{
# Two persistent regimes: low (0.1) vs high (0.8) correlation
sim <- rsdc_simulate(n = 500, X = matrix(1, 500, 1),
                     beta = matrix(qlogis(0.9), 2, 1),
                     mu = matrix(0, 2, 2),
                     sigma = array(c(1, 0.1, 0.1, 1,
                                     1, 0.8, 0.8, 1), c(2, 2, 2)),
                     N = 2, seed = 2)
fit <- rsdc_estimate("noX", residuals = sim$observations, N = 2)
summary(fit)
confint(fit)
c(AIC = AIC(fit), BIC = BIC(fit))
plot(fit)
}

}
\seealso{
\code{\link{rsdc_estimate}} for fitting;
\code{\link{rsdc_forecast}}, \code{\link{rsdc_bootstrap}}.
}
