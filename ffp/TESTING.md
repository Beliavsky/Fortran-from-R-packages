# Testing

Run `fpm test`. The tests cover normalization, exponential decay, half-life,
weighted moments, entropy pooling under a mean view, relative entropy,
scenario effectiveness, Gaussian kernels, and double-decay covariance.
