# Build report

Compiler used for validation: GNU Fortran 14.2.0.

Validated targets:

- strict checked library build
- five checked runtime tests
- optimized library build
- five optimized runtime tests
- demonstration application
- curve example
- TOML parsing of `fpm.toml`
- duplicate module/program-name scan across FPM source roots
- independent extraction and rebuild of the final ZIP
