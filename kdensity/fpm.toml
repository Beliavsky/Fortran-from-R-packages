name = "kdensity"
version = "0.1.0"
license = "MIT"
author = "Fortran translation contributors"
maintainer = "Fortran translation contributors"
description = "Parametrically guided kernel density estimation with asymmetric kernels"
categories = ["statistics", "density-estimation"]
keywords = ["kernel-density", "asymmetric-kernels", "parametric-starts"]

[build]
auto-executables = true
auto-tests = true
auto-examples = true
module-naming = false

[extra.r-fpm-index]
gams = ["L"]
